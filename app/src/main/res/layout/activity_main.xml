<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#000000"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BENVENUTO IN LEGGO"
        android:textColor="#FFD700"
        android:textSize="40sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="32dp"/>
        
    <!-- Questa immagine è opzionale nella MainActivity (schermata di benvenuto iniziale), 
         ma se presente deve essere gestita. 
         Attualmente il layout XML letto NON ha un'ImageView.
         Se l'utente si riferisce alla "home page" come activity_home_final.xml (che è il layout della Home reale),
         dobbiamo guardare quello. -->

    <Button
        android:id="@+id/btnTestReader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vai al Lettore (Test)"/>

</LinearLayout>