<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Leggo"
        tools:replace="android:theme,android:allowBackup,android:label,android:icon,android:roundIcon,android:supportsRtl,android:appComponentFactory,android:dataExtractionRules,android:fullBackupContent"
        tools:ignore="MissingClass">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".SettingsActivity" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".SearchBooksActivity" />
        
        <!-- DocumentActivity è spesso definita nell'AAR di MuPDF. 
             La dichiariamo qui per poter fare override degli attributi se necessario -->
        <activity android:name="com.artifex.mupdf.viewer.DocumentActivity"
            android:exported="true"
            tools:node="merge"
            tools:replace="android:exported,android:theme">
        </activity>

        <activity android:name=".PdfReaderActivity" />
        <activity android:name=".TrashActivity" />
        <activity android:name=".CreditsActivity" />
        
        <service 
            android:name=".ReadingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

    </application>

</manifest>